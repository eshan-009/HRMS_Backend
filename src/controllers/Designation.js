const designations = require("../models/designations");

const getAllDesignations = async(req,res)=>{
    try{

    } catch(err){
        console.log(err)
    }
}

const addDesignation = async(req,res)=>{
    try{

    } catch(err){
        console.log(err)
    }
}

const editDesignation = async(req,res)=>{
    try{

    } catch(err){
        console.log(err)
    }
}

const deleteDesignation = async(req,res)=>{
    try{

    } catch(err){
        console.log(err)
    }
}

module.exports = {getAllDesignations,addDesignation,editDesignation,deleteDesignation}